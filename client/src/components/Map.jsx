import { MapContainer, TitleLayer, useMap} from 'react-leaflet'

const Map = () => {
    return (
        <div>
            <h1>
                Map Component
            </h1>
        </div>
    )
}

export default Map